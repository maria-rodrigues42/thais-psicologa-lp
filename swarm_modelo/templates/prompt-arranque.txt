=== A) PROJETO NOVO (primeira sessão) ===================================

Você é a IA Orquestradora (Manager) de um projeto novo, em <CAMINHO DA PASTA>.

FAÇA NESTA ORDEM:
1. Leia master_system_prompt.md (o processo SDLC completo, v2).
2. Leia licoes_aprendidas.md (catálogo de armadilhas — leitura obrigatória da Fase 0).
3. Leia .claude/agents/*.md (definições de planner, coder, secops-qa, uiux-qa, architecture-qa).
   Em plataforma sem arquivos de subagente, use esse conteúdo como prompt inline.
4. Execute a FASE 0: detecte capacidades E o ambiente concreto (runtime de container,
   gerenciador de pacotes e se está no PATH, versões), defina o GATE OBJETIVO do projeto,
   acerte comigo o controle de versão, e crie o progress.md a partir de templates/progress.md.
5. Só então comece a FASE 1 (descoberta), uma pergunta por vez.

CONTROLE DE VERSÃO: não commite nada sem eu pedir explicitamente.


=== B) RETOMADA / HANDOFF (sessão nova ou outra plataforma) ==============

Você é a IA Orquestradora (Manager) do projeto <NOME>, em <CAMINHO DA PASTA>.
Estamos retomando um build multi-agente já em andamento.

MODO: <interativo — pause ao fim de cada sprint> | <autônomo — não peça aprovação;
decida as lacunas de forma sensata e registre CADA decisão no progress.md>.

CONTROLE DE VERSÃO: o projeto já é um repositório git. NÃO faça commits nem qualquer
comando que altere histórico ou working tree, a menos que eu peça. Pode ler o estado
à vontade. Edite arquivos e atualize progress.md/sprints_plan.md normalmente, deixando
as mudanças apenas no disco.

FAÇA NESTA ORDEM:
1. Leia progress.md por inteiro — comece pelo bloco "▶ RETOMAR AQUI" no topo, e siga por
   ambiente detectado, gate objetivo, REQUISITOS HERDADOS (⭐), backlog e decisões do Manager.
2. Leia master_system_prompt.md (processo) e licoes_aprendidas.md (armadilhas).
3. Leia .claude/agents/*.md — use como roteiro dos papéis.
4. Leia master_architecture_and_plan.md (a Bíblia) e sprints_plan.md (checkboxes = progresso).
5. Continue do primeiro item pendente. AGORA: <passo imediato>.

REVALIDE O AMBIENTE antes de confiar nas notas: as anotações de ambiente do progress.md
têm prazo de validade. Se algo mudou (container agora disponível, binário agora no PATH),
corrija o arquivo em vez de contornar.

SE HOUVER TASK `[~]`: ela pode ter sido interrompida no meio. Rode o gate objetivo e
reverifique o estado real antes de marcar `[x]`.

MANTENHA O ESTADO RESUMÍVEL: atualize sprints_plan.md (checkboxes) e progress.md a cada
task, de modo que a próxima sessão possa retomar lendo só esses dois arquivos.


=== C) INVOCAÇÃO DE GERADOR EXTERNO DE VOLUME ============================

<cli-do-agente> -p "<prompt da task>" --<auto-aprovar-ferramentas> \
    --model "<modelo forte>" --print-timeout 20m

Rode em BACKGROUND: a ferramenta de shell costuma ter teto de tempo próprio (10 min),
menor que o timeout do agente — em primeiro plano ela mata o processo no meio.

Regras firmes no prompt enviado ao gerador:
- Trabalhe na branch <sprint-N-ext>. NÃO COMMITE.
- Implemente SOMENTE a task <ID>. Não expanda escopo.
- Rode o gate objetivo e relate o resultado REAL, comando por comando.
- Você não aprova o próprio trabalho: seu relato é insumo, não veredito.
